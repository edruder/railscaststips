= simple_form_for @treasure do |f|
  = f.association :railscast
  = f.input :description
  = f.input :time
  = f.input :tag_list
  .actions
    .pull-right
      = f.submit 'Save', class: 'btn btn-flat flat-inverse btn-rounded'
      = link_to 'Cancel', :back , class: 'btn btn-warning'
